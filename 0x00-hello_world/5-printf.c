#include <stdio.h>
/**
 * main - its the start of a program
 *
 * return : Always 0 (success), non-zero otherwise
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
