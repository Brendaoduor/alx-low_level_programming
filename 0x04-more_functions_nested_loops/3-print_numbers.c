#include "main.h"

/**
 * print_numbers - function prints numbers from 0 to 9
 * @a: passes parameters
 * Return: returns 0
 */
void print_numbers(void)
{
int;
for (a = '0'; a <= '9'; a++)
_putchar(a);
_putchar('\n');
return (0);
}

